export const environment = {
  production: true,
  // Same-origin in production (Angular build served behind the same reverse
  // proxy/host as the API) — override at build time if that's ever not true.
  apiBaseUrl: '/api/v1',
};
